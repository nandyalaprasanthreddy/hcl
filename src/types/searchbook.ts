export interface Book {
  bookid: number;
  title: string;
  author: string;
  category: string;
  status: string;
  renewDate: string;
}

export interface BookHeader {
  key: string;
  header: string;
}
